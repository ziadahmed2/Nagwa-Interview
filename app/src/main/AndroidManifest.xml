<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mvvm">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.example.mvvm.BaseMvvmApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NagwaInterview">

        <activity android:name="com.example.mvvm.view.home.VideoActivity" android:configChanges="orientation|uiMode|screenSize"/>
        <activity android:name="com.example.mvvm.view.home.PdfActivity" android:screenOrientation="portrait"/>
        <activity
            android:name="com.example.mvvm.view.home.HomeActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>